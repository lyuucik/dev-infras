---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: restart dotnet-app
  ansible.builtin.systemd:
    name: "{{ dotnet_app_name }}"
    state: restarted

- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
